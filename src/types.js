/* @flow */
export type Namespace$AppState = {
  clicks: number,
};

export type Namespace$DetailParams = {
  params: {
    id: string,
  },
};

export type Namespace$WebpackConfig = {
  output: {
    publicPath: string,
  },
};
